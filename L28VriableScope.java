package l23;

// local = declared inside a method. visible only to that method
// global = declared outside a method but within a class. visible to all parts of a class
public class L28VriableScope {

	public static void main(String[] args) {
		// create an object of the class DiceRoller
		DiceRoller diceRoller = new DiceRoller(); // this will call the roll method

	}

}
